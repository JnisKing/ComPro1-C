#include<stdio.h>
int main(){
    int x,y,z;
    scanf("%d %d %d",&x,&y,&z);
    if(x<y||x<z||y<z){
        printf("%d %d %d",x,y,z);
    }
    else if(y<x||y<z||x<z){
        printf("%d %d %d",y,x,z);
    }
}
